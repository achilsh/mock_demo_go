//如何对sqlite和gorm 语法做单元测试，目标就是：避免对业务db 写脏数据，同时也要检查sql的语法的正确性。
//有些思想可以达到，比如： 在gorm打开db时，把原来的 db name 文件 替换成  "file::memory:?cache=shared" ；
//这样操作数据库就是内存数据库。单元用例测试跑完后，内存就会回收，就不会对数据文件产生污染，同时也可以验证我们的sql语句是否正确。
//在单元测试的时候封装一个接口，该接口就是创建一个新的的db handle 初始化全局句柄。
//在单元测试开始时调用该接口即可。